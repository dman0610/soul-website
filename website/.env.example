# ── Claude API ── server-side only, never in client components ────────────────
# Get your key from: console.anthropic.com → API Keys
# Add production value in: Vercel → Settings → Environment Variables
ANTHROPIC_API_KEY=

# ── Booking Integration (future) ──────────────────────────────────────────────
# Connect FareHarbor, Square, Bokun, Checkfront, etc.
# See src/app/api/bookings/route.ts for implementation notes.
# BOOKING_API_KEY=
# BOOKING_API_URL=
